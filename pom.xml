<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.biit</groupId>
    <artifactId>fact-manager</artifactId>
    <version>0.1</version>
    <name>FactManager</name>
    <modules>
        <module>fact-manager-logger</module>
        <module>fact-manager-persistence</module>
        <module>fact-manager-core</module>
        <module>fact-manager-rest</module>
    </modules>

    <packaging>pom</packaging>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.2.4.RELEASE</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <profiles>
        <profile>
            <id>dev-azure-com-maisteringbv-mbv-val-artifacts</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <distributionManagement>
                <repository>
                    <id>dev-azure-com-maisteringbv-mbv-val-artifacts</id>
                    <name>libs-releases</name>
                    <url>https://pkgs.dev.azure.com/maisteringbv/_packaging/MBV-VAL-ARTIFACTS/maven/v1</url>
                </repository>
            </distributionManagement>
        </profile>
    </profiles>

    <repositories>
        <repository>
            <id>dev-azure-com-maisteringbv-mbv-val-artifacts</id>
            <url>https://pkgs.dev.azure.com/maisteringbv/_packaging/MBV-VAL-ARTIFACTS/maven/v1</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <finalName>factManager</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>8</source>
                    <target>8</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${checkstyle.version}</version>
                <configuration>
                    <configLocation>${main.basedir}/checkstyle.xml</configLocation>
                    <encoding>UTF-8</encoding>
                    <consoleOutput>true</consoleOutput>
                    <failsOnError>true</failsOnError>
                    <linkXRef>false</linkXRef>
                </configuration>
                <executions>
                    <execution>
                        <id>validate</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
                <version>${spotbugs.version}</version>
                <executions>
                    <execution>
                        <phase>compile</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                        <configuration>
                            <!-- Enables analysis which takes more memory but finds more bugs.
                                If you run out of memory, changes the value of the effort element to 'Low'. -->
                            <effort>Max</effort>
                            <!-- Build fail if problems are found -->
                            <failOnError>true</failOnError>
                            <!-- Reports all bugs (values are max, medium and max) -->
                            <threshold>max</threshold>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <properties>
        <azure.version>1.23.0</azure.version>
        <biit-common-utils.version>[0.4,0.5)</biit-common-utils.version>
        <checkstyle.version>2.17</checkstyle.version>
        <findbug.version>3.0.5</findbug.version>
        <spotbugs.version>4.0.4</spotbugs.version>
        <log4j.version>1.2.17</log4j.version>
        <main.basedir>${project.basedir}</main.basedir>
        <microsoft-insights.version>1.1.2</microsoft-insights.version>
        <org.testng.version>6.8.13</org.testng.version>
        <spring-boot.version>2.1.8.RELEASE</spring-boot.version>
        <springfox-swagger.version>2.9.2</springfox-swagger.version>
        <action-underlying-model.version>[0.6,0.7)</action-underlying-model.version>
        <azure-storage.version>8.4.0</azure-storage.version>
        <postgresql.version>42.2.10</postgresql.version>
    </properties>

</project>